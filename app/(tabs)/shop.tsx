import Layout from "@/components/Layout";
import React from "react";
import { Text, View } from "react-native";

const Shop = () => {
    return (
        <Layout>
            <View>
                <Text className="text-2xl font-bold">Shop</Text>
            </View>
        </Layout>
    );
};

export default Shop;
