import Layout from "@/components/Layout";
import React from "react";
import { Text } from "react-native";

const broadbandSettings = () => {
  return (
    <Layout>
      <Text>broadbandSettings</Text>
    </Layout>
  );
};

export default broadbandSettings;
