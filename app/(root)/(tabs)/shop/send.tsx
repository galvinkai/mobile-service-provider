import Layout from "@/components/Layout";
import React from "react";
import { Text } from "react-native";

const send = () => {
  return (
    <Layout>
      <Text>send</Text>
    </Layout>
  );
};

export default send;
